services:
  phpinfo:
    deploy:
      mode: replicated
      placement:
        constraints:
          - node.role == worker
      replicas: 2
    image: rabellaatbsc/simplilearn-phpinfo:latest
    ports:
      - 80:8080
version:n '3.8'
